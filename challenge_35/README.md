# Challenge 35 — Study Checklist

To solve this challenge, you should understand:
- Finite state machine concepts: states, events, transitions, terminal states
- Table-driven design (control flow as data)
- `enum.StrEnum` and how it differs from `Enum`
- Tuple dict keys for composite lookup
- `getattr`-based convention hooks (and their typo risk)
- Custom exceptions carrying structured context
- Why explicit transition tables beat if/elif chains

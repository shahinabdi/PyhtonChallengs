# Challenge 32 — Study Checklist

To solve this challenge, you should understand:
- Dependency injection and inversion of control
- Constructor injection vs hard-wired construction
- `typing.Protocol` for capability-based dependency types
- The composition root pattern (`build_app`)
- Hand-written fakes vs `unittest.mock`
- Interface segregation: depend only on the methods you call
- Why "default = prod instance" arguments undermine DI
